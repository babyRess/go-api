version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./credentials:/root/credentials:ro
    environment:
      - GIN_MODE=release
    restart: unless-stopped 